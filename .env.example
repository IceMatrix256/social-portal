# Social Portal Environment Configuration

# Custom CORS Proxy (optional)
# Set this to use your own self-hosted CORS proxy instead of public services
# Example: VITE_CORS_PROXY_URL=https://your-proxy.example.com/
VITE_CORS_PROXY_URL=

# P2P sync (Gun.js)
# By default Gun runs local-only (no peers). To enable cross-device sync, set one or more peers:
# VITE_GUN_PEERS=https://your-gun-relay.example/gun,https://another-relay.example/gun
VITE_GUN_PEERS=

# Desktop bridge settings (used by scripts/server.py when running npm run backend)
# Comma-separated full URLs, self-hosted entries are tried before public instances.
# SOCIAL_PORTAL_REDLIB_SELF_HOSTED=http://127.0.0.1:8082
# SOCIAL_PORTAL_REDLIB_PUBLIC=https://l.opnxng.com,https://redlib.catsarch.com
# SOCIAL_PORTAL_ENABLE_REDLIB_BRIDGE=true
# SOCIAL_PORTAL_REQUEST_TIMEOUT_SECONDS=6
# SOCIAL_PORTAL_MAX_SOURCE_ATTEMPTS=4
# SOCIAL_PORTAL_MAX_PROXY_FALLBACK_ATTEMPTS=2
